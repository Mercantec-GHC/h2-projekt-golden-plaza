import React, { useEffect, useState } from 'react';
import './Home.css';

// Home page component
const Home = () => {
    
    // Clean page with no content
    // Used to hold main booking options but this was moved to the Booking page
    return (
        <div className="background">
            <div className="card-container">
            </div>
        </div>
    );
};

export default Home;
